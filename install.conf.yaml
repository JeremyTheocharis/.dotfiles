- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.vimrc: vimrc
    ~/.vim: vim
    ~/.fonts: fonts
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
    ~/.oh-my-zsh: oh-my-zsh
    ~/.config/nvim/init.vim: neovim
    ~/.config/lazygit/config.yaml: lazygit
    #- shell:
    #- dconf load /org/gnome/terminal/legacy/profiles:/ < gnome-terminal-profiles.dconf
    #- fc-cache -f -v


